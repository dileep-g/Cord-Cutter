  <p style="margin-top: 5%" class="show">
    <strong>Package name:</strong>
    <%= @package.name %>
    <br>
    <% if @package.DVR %>
      <p>Support DVR service</p>
    <% else %>
      <p>Does not support DVR service</p>
    <% end %>
    <ul><li>Package cost: $<%= @package.cost %></li></ul>
    <ul><li>Include channels: </li></ul>
      <% if @package_channels != nil %>
        <ul>
          <table width=100%>
            <tbody>
              <% @package_channels.each do |channel| %>
                <tr class="col-xs-10">
                  <ul class="list-unstyled">
                    <div class="col-xs-offset-0 col-xs-2 text-left">&diams; &nbsp;<span style="font-size: 11px"><%= channel.name %><br> <br/></span></div>
                  </ul>  
                </tr>
              <% end %>
            </tbody>
        </table>
        </ul>
      <% end %>
      
    <ul><li>Available devices: </li></ul>
     <% if @package_devices != nil %>
        <ul>
          <table width=100%>
            <tbody>
              <% @package_devices.each do |device| %>
                <tr class="col-xs-10">
                  <ul class="list-unstyled">
                    <div class="col-xs-offset-0 col-xs-2 text-left">&diams; &nbsp;<span style="font-size: 11px"><%= device.name %><br> <br/></span></div>
                  </ul>  
                </tr>
              <% end %>
            </tbody>
        </table>
        </ul>
      <% end %>
    <ul><li>Available set top boxes: </li></ul>
     <% if @package_boxes != nil %>
        <ul>
          <table width=100%>
            <tbody>
              <% @package_boxes.each do |box| %>
                <tr class="col-xs-10">
                  <ul class="list-unstyled">
                    <div class="col-xs-offset-0 col-xs-2 text-left">&diams; &nbsp;<span style="font-size: 11px"><%= box.name %><br> <br/></span></div>
                  </ul>  
                </tr>
              <% end %>
            </tbody>
        </table>
        </ul>
      <% end %>
  </p>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Add New Package', new_package_path %> |
<%= link_to 'Add New Hierarchical Package', hierarchical_new_path %> |
<%= link_to 'Back', packages_path %>
